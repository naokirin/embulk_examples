{% include 'consts' %}
{% include 'in' %}

filters:
  - type: column
    columns:
      - {name: id, type: long}
      - {name: user_name, src: value}
out:
  type: command
  command: "cat - > '{{ output_path }}'"
  formatter:
    type: fast_jsonl

